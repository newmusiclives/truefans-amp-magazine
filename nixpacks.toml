[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[variables]
PYTHONPATH = "/app/src"

[start]
cmd = "cd /app && python -m uvicorn weeklyamp.web.app:create_app --host 0.0.0.0 --port ${PORT:-8000} --factory"
